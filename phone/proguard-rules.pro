# :phone module — no keep rules needed yet. This module has no code to shrink/obfuscate in this
# scaffold batch (watch-osd-message-delivery / Batch 2). Add rules here alongside whichever batch
# introduces a dependency that needs one (none expected — see design decision #1: zero new deps).
