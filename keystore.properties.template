# Template for keystore.properties — copy this file to `keystore.properties` (repo root) and fill
# in real values. `keystore.properties` is gitignored (see .gitignore) — NEVER commit real signing
# secrets.
#
# Shared release signing key for :wear and :phone (see signing.gradle.kts). Both modules must be
# signed with the SAME key so the Wear Data Layer AppKey matches (design decision #1,
# watch-osd-message-delivery / Option F).
#
# All values below are placeholders — none of this is a real secret.

storeFile=../seizureguard-release.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
