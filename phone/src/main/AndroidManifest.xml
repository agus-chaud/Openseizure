<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Envío de SMS de emergencia al cuidador (se usa en Fase 3.3) -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Llamadas de emergencia (Fase 3.3) -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Mantener pantalla encendida para la alarma full-screen (Fase 3.2) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Necesario para que la alarma full-screen funcione sobre la lock screen -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Internet para el Data Layer con el watch -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            DataLayerListenerService — se agrega en Fase 3.1
            <service
                android:name=".service.DataLayerListenerService"
                android:exported="true">
                <intent-filter>
                    <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                    <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                </intent-filter>
            </service>
        -->

    </application>

</manifest>
