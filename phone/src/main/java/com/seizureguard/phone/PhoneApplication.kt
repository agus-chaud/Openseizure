package com.seizureguard.phone

import android.app.Application

/**
 * Application class placeholder — watch-osd-message-delivery / Option F, Batch 2 (scaffold).
 *
 * Intentionally empty for now: no bridge logic lives here yet. Declared early so later batches
 * (OsdBridgeService, notification channel setup, etc. — Batch 5+) have a stable hook without a
 * manifest change. See openspec/changes/watch-osd-message-delivery/design.md.
 */
class PhoneApplication : Application()
